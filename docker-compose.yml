services:
  gcore-monitor:
    image: hteppl/gcore-dns-monitor:latest
    container_name: gcore-dns-monitor
    restart: unless-stopped
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
